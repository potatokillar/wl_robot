# qr_wl 快速开始指南

## 🚀 一键配置（新人专用）

如果你是第一次使用本项目，只需运行一个命令即可完成所有环境配置：

```bash
./setup_environment.sh
```

这个脚本会自动完成以下任务：
1. ✅ 检查 Docker 安装
2. ✅ 配置 Harbor 不安全仓库
3. ✅ 自动登录 Harbor（账号密码已内置）
4. ✅ 下载编译所需的 Docker 镜像
5. ✅ 验证环境配置

## 📋 先决条件

- Ubuntu 20.04+ 或其他 Linux 发行版
- 网络连接到 192.168.1.93（Harbor 仓库）
- sudo 权限（用于配置 Docker）

## 🔧 详细步骤

### 1. 克隆项目

```bash
git clone http://192.168.1.55/ontology/qr_wl.git
cd qr_wl
```

### 2. 运行环境配置脚本

```bash
./setup_environment.sh
```

脚本会引导你完成整个配置过程，包括：
- 自动检测系统环境
- 安装缺失的依赖
- 配置 Docker daemon
- 登录 Harbor 仓库
- 拉取编译镜像

### 3. 编译项目

环境配置完成后，可以使用以下命令编译：

```bash
# Docker 编译（推荐）
./build_docker.sh

# 编译特定架构
./build_docker.sh x86    # x86_64 架构
./build_docker.sh arm    # ARM64 架构

# 清理编译
./build_docker.sh -c

# 强制更新镜像并编译
./build_docker.sh --pull
```

## 🐳 Docker 镜像信息

- **镜像地址**：`192.168.1.93/iiri/build_x86_arm_ros1:latest`
- **Harbor 仓库**：http://192.168.1.93
- **登录账号**：admin
- **登录密码**：Westlake1234

## 🔍 常见问题

### Docker 未安装

如果系统没有安装 Docker，请先安装：

```bash
# Ubuntu/Debian
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER
newgrp docker
```

### Harbor 连接失败

如果无法连接到 Harbor 仓库：
1. 检查网络连接：`ping 192.168.1.93`
2. 检查 Harbor 服务：`curl http://192.168.1.93`
3. 确认防火墙设置允许访问

### 镜像拉取失败

如果镜像拉取失败：
1. 确认已登录 Harbor：`docker login 192.168.1.93`
2. 检查镜像是否存在：访问 http://192.168.1.93 查看
3. 重新运行：`./setup_environment.sh`

## 📚 更多信息

- **项目文档**：[README.md](README.md)
- **构建系统**：[build_docker.sh](build_docker.sh)
- **版本管理**：[VERSION_MANAGEMENT.md](VERSION_MANAGEMENT.md)
- **CI/CD 配置**：[docs/ci-cd/jenkins-setup.md](docs/ci-cd/jenkins-setup.md)

## 💡 提示

- 首次使用建议运行 `./setup_environment.sh` 完成完整配置
- 镜像较大（约 2-3GB），首次下载需要一定时间
- 配置完成后，后续编译无需重复配置
- 使用 `docker images` 查看已下载的镜像

---

**作者**：唐文浩
**更新日期**：2025-10-15